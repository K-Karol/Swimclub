<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Swimclub.Mobile.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <!--<Entry x:Name="UsernameInput" ClearButtonVisibility="WhileEditing" Placeholder="Username" IsTextPredictionEnabled="false" Text=""/> Dont know why this one doesnt work but...-->
            <Entry x:Name="UsernameInput" ClearButtonVisibility="WhileEditing" Placeholder="Username" IsTextPredictionEnabled="false" Text=""/>
            <Entry x:Name="PasswordInput" ClearButtonVisibility="WhileEditing" Placeholder="Password" IsPassword="true" IsTextPredictionEnabled="false" Text=""/>
            <Button x:Name="LoginButton"  VerticalOptions="Center" Text="Login" Clicked="LoginButton_Clicked" Command="{Binding LoginCommand}"/> <!--Bindings never used here, not sure if i should remove it thou so left it in for now-->
            <Button x:Name="passwordForgoten" VerticalOptions="Center" Clicked="passwordForgoten_Clicked" Text="Forgot Password?" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>